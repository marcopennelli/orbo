// Code generated by goa v3.24.1, DO NOT EDIT.
//
// orbo HTTP client CLI support package
//
// Command:
// $ goa gen orbo/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	authc "orbo/gen/http/auth/client"
	camerac "orbo/gen/http/camera/client"
	configc "orbo/gen/http/config/client"
	healthc "orbo/gen/http/health/client"
	motionc "orbo/gen/http/motion/client"
	systemc "orbo/gen/http/system/client"
	"os"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"health (healthz|readyz)",
		"auth (login|status)",
		"camera (list|get|create|update|delete|activate|deactivate|capture|enable-alerts|disable-alerts)",
		"motion (events|event|frame|forensic-thumbnail)",
		"config (get|update|test-notification|get-dinov3|update-dinov3|test-dinov3|get-yolo|update-yolo|test-yolo|get-detection|update-detection|get-pipeline|update-pipeline|get-recognition|update-recognition|test-recognition)",
		"system (status|start-detection|stop-detection)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "health healthz" + "\n" +
		os.Args[0] + " " + "auth login --body '{\n      \"password\": \"Quia repudiandae neque magni.\",\n      \"username\": \"Eum cum et cumque.\"\n   }'" + "\n" +
		os.Args[0] + " " + "camera list" + "\n" +
		os.Args[0] + " " + "motion events --camera-id \"66f585c5-f085-411f-9cc1-ae134b05e642\" --since \"1976-02-05T16:52:43Z\" --limit -3417244801850337904" + "\n" +
		os.Args[0] + " " + "config get" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		healthFlags = flag.NewFlagSet("health", flag.ContinueOnError)

		healthHealthzFlags = flag.NewFlagSet("healthz", flag.ExitOnError)

		healthReadyzFlags = flag.NewFlagSet("readyz", flag.ExitOnError)

		authFlags = flag.NewFlagSet("auth", flag.ContinueOnError)

		authLoginFlags    = flag.NewFlagSet("login", flag.ExitOnError)
		authLoginBodyFlag = authLoginFlags.String("body", "REQUIRED", "")

		authStatusFlags = flag.NewFlagSet("status", flag.ExitOnError)

		cameraFlags = flag.NewFlagSet("camera", flag.ContinueOnError)

		cameraListFlags = flag.NewFlagSet("list", flag.ExitOnError)

		cameraGetFlags  = flag.NewFlagSet("get", flag.ExitOnError)
		cameraGetIDFlag = cameraGetFlags.String("id", "REQUIRED", "Camera ID")

		cameraCreateFlags    = flag.NewFlagSet("create", flag.ExitOnError)
		cameraCreateBodyFlag = cameraCreateFlags.String("body", "REQUIRED", "")

		cameraUpdateFlags    = flag.NewFlagSet("update", flag.ExitOnError)
		cameraUpdateBodyFlag = cameraUpdateFlags.String("body", "REQUIRED", "")
		cameraUpdateIDFlag   = cameraUpdateFlags.String("id", "REQUIRED", "Camera ID")

		cameraDeleteFlags  = flag.NewFlagSet("delete", flag.ExitOnError)
		cameraDeleteIDFlag = cameraDeleteFlags.String("id", "REQUIRED", "Camera ID")

		cameraActivateFlags  = flag.NewFlagSet("activate", flag.ExitOnError)
		cameraActivateIDFlag = cameraActivateFlags.String("id", "REQUIRED", "Camera ID")

		cameraDeactivateFlags  = flag.NewFlagSet("deactivate", flag.ExitOnError)
		cameraDeactivateIDFlag = cameraDeactivateFlags.String("id", "REQUIRED", "Camera ID")

		cameraCaptureFlags  = flag.NewFlagSet("capture", flag.ExitOnError)
		cameraCaptureIDFlag = cameraCaptureFlags.String("id", "REQUIRED", "Camera ID")

		cameraEnableAlertsFlags  = flag.NewFlagSet("enable-alerts", flag.ExitOnError)
		cameraEnableAlertsIDFlag = cameraEnableAlertsFlags.String("id", "REQUIRED", "Camera ID")

		cameraDisableAlertsFlags  = flag.NewFlagSet("disable-alerts", flag.ExitOnError)
		cameraDisableAlertsIDFlag = cameraDisableAlertsFlags.String("id", "REQUIRED", "Camera ID")

		motionFlags = flag.NewFlagSet("motion", flag.ContinueOnError)

		motionEventsFlags        = flag.NewFlagSet("events", flag.ExitOnError)
		motionEventsCameraIDFlag = motionEventsFlags.String("camera-id", "", "")
		motionEventsSinceFlag    = motionEventsFlags.String("since", "", "")
		motionEventsLimitFlag    = motionEventsFlags.String("limit", "50", "")

		motionEventFlags  = flag.NewFlagSet("event", flag.ExitOnError)
		motionEventIDFlag = motionEventFlags.String("id", "REQUIRED", "Event ID")

		motionFrameFlags  = flag.NewFlagSet("frame", flag.ExitOnError)
		motionFrameIDFlag = motionFrameFlags.String("id", "REQUIRED", "Event ID")

		motionForensicThumbnailFlags     = flag.NewFlagSet("forensic-thumbnail", flag.ExitOnError)
		motionForensicThumbnailIDFlag    = motionForensicThumbnailFlags.String("id", "REQUIRED", "Event ID")
		motionForensicThumbnailIndexFlag = motionForensicThumbnailFlags.String("index", "REQUIRED", "Face thumbnail index (0-based)")

		configFlags = flag.NewFlagSet("config", flag.ContinueOnError)

		configGetFlags = flag.NewFlagSet("get", flag.ExitOnError)

		configUpdateFlags    = flag.NewFlagSet("update", flag.ExitOnError)
		configUpdateBodyFlag = configUpdateFlags.String("body", "REQUIRED", "")

		configTestNotificationFlags = flag.NewFlagSet("test-notification", flag.ExitOnError)

		configGetDinov3Flags = flag.NewFlagSet("get-dinov3", flag.ExitOnError)

		configUpdateDinov3Flags    = flag.NewFlagSet("update-dinov3", flag.ExitOnError)
		configUpdateDinov3BodyFlag = configUpdateDinov3Flags.String("body", "REQUIRED", "")

		configTestDinov3Flags = flag.NewFlagSet("test-dinov3", flag.ExitOnError)

		configGetYoloFlags = flag.NewFlagSet("get-yolo", flag.ExitOnError)

		configUpdateYoloFlags    = flag.NewFlagSet("update-yolo", flag.ExitOnError)
		configUpdateYoloBodyFlag = configUpdateYoloFlags.String("body", "REQUIRED", "")

		configTestYoloFlags = flag.NewFlagSet("test-yolo", flag.ExitOnError)

		configGetDetectionFlags = flag.NewFlagSet("get-detection", flag.ExitOnError)

		configUpdateDetectionFlags    = flag.NewFlagSet("update-detection", flag.ExitOnError)
		configUpdateDetectionBodyFlag = configUpdateDetectionFlags.String("body", "REQUIRED", "")

		configGetPipelineFlags = flag.NewFlagSet("get-pipeline", flag.ExitOnError)

		configUpdatePipelineFlags    = flag.NewFlagSet("update-pipeline", flag.ExitOnError)
		configUpdatePipelineBodyFlag = configUpdatePipelineFlags.String("body", "REQUIRED", "")

		configGetRecognitionFlags = flag.NewFlagSet("get-recognition", flag.ExitOnError)

		configUpdateRecognitionFlags    = flag.NewFlagSet("update-recognition", flag.ExitOnError)
		configUpdateRecognitionBodyFlag = configUpdateRecognitionFlags.String("body", "REQUIRED", "")

		configTestRecognitionFlags = flag.NewFlagSet("test-recognition", flag.ExitOnError)

		systemFlags = flag.NewFlagSet("system", flag.ContinueOnError)

		systemStatusFlags = flag.NewFlagSet("status", flag.ExitOnError)

		systemStartDetectionFlags = flag.NewFlagSet("start-detection", flag.ExitOnError)

		systemStopDetectionFlags = flag.NewFlagSet("stop-detection", flag.ExitOnError)
	)
	healthFlags.Usage = healthUsage
	healthHealthzFlags.Usage = healthHealthzUsage
	healthReadyzFlags.Usage = healthReadyzUsage

	authFlags.Usage = authUsage
	authLoginFlags.Usage = authLoginUsage
	authStatusFlags.Usage = authStatusUsage

	cameraFlags.Usage = cameraUsage
	cameraListFlags.Usage = cameraListUsage
	cameraGetFlags.Usage = cameraGetUsage
	cameraCreateFlags.Usage = cameraCreateUsage
	cameraUpdateFlags.Usage = cameraUpdateUsage
	cameraDeleteFlags.Usage = cameraDeleteUsage
	cameraActivateFlags.Usage = cameraActivateUsage
	cameraDeactivateFlags.Usage = cameraDeactivateUsage
	cameraCaptureFlags.Usage = cameraCaptureUsage
	cameraEnableAlertsFlags.Usage = cameraEnableAlertsUsage
	cameraDisableAlertsFlags.Usage = cameraDisableAlertsUsage

	motionFlags.Usage = motionUsage
	motionEventsFlags.Usage = motionEventsUsage
	motionEventFlags.Usage = motionEventUsage
	motionFrameFlags.Usage = motionFrameUsage
	motionForensicThumbnailFlags.Usage = motionForensicThumbnailUsage

	configFlags.Usage = configUsage
	configGetFlags.Usage = configGetUsage
	configUpdateFlags.Usage = configUpdateUsage
	configTestNotificationFlags.Usage = configTestNotificationUsage
	configGetDinov3Flags.Usage = configGetDinov3Usage
	configUpdateDinov3Flags.Usage = configUpdateDinov3Usage
	configTestDinov3Flags.Usage = configTestDinov3Usage
	configGetYoloFlags.Usage = configGetYoloUsage
	configUpdateYoloFlags.Usage = configUpdateYoloUsage
	configTestYoloFlags.Usage = configTestYoloUsage
	configGetDetectionFlags.Usage = configGetDetectionUsage
	configUpdateDetectionFlags.Usage = configUpdateDetectionUsage
	configGetPipelineFlags.Usage = configGetPipelineUsage
	configUpdatePipelineFlags.Usage = configUpdatePipelineUsage
	configGetRecognitionFlags.Usage = configGetRecognitionUsage
	configUpdateRecognitionFlags.Usage = configUpdateRecognitionUsage
	configTestRecognitionFlags.Usage = configTestRecognitionUsage

	systemFlags.Usage = systemUsage
	systemStatusFlags.Usage = systemStatusUsage
	systemStartDetectionFlags.Usage = systemStartDetectionUsage
	systemStopDetectionFlags.Usage = systemStopDetectionUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "health":
			svcf = healthFlags
		case "auth":
			svcf = authFlags
		case "camera":
			svcf = cameraFlags
		case "motion":
			svcf = motionFlags
		case "config":
			svcf = configFlags
		case "system":
			svcf = systemFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "health":
			switch epn {
			case "healthz":
				epf = healthHealthzFlags

			case "readyz":
				epf = healthReadyzFlags

			}

		case "auth":
			switch epn {
			case "login":
				epf = authLoginFlags

			case "status":
				epf = authStatusFlags

			}

		case "camera":
			switch epn {
			case "list":
				epf = cameraListFlags

			case "get":
				epf = cameraGetFlags

			case "create":
				epf = cameraCreateFlags

			case "update":
				epf = cameraUpdateFlags

			case "delete":
				epf = cameraDeleteFlags

			case "activate":
				epf = cameraActivateFlags

			case "deactivate":
				epf = cameraDeactivateFlags

			case "capture":
				epf = cameraCaptureFlags

			case "enable-alerts":
				epf = cameraEnableAlertsFlags

			case "disable-alerts":
				epf = cameraDisableAlertsFlags

			}

		case "motion":
			switch epn {
			case "events":
				epf = motionEventsFlags

			case "event":
				epf = motionEventFlags

			case "frame":
				epf = motionFrameFlags

			case "forensic-thumbnail":
				epf = motionForensicThumbnailFlags

			}

		case "config":
			switch epn {
			case "get":
				epf = configGetFlags

			case "update":
				epf = configUpdateFlags

			case "test-notification":
				epf = configTestNotificationFlags

			case "get-dinov3":
				epf = configGetDinov3Flags

			case "update-dinov3":
				epf = configUpdateDinov3Flags

			case "test-dinov3":
				epf = configTestDinov3Flags

			case "get-yolo":
				epf = configGetYoloFlags

			case "update-yolo":
				epf = configUpdateYoloFlags

			case "test-yolo":
				epf = configTestYoloFlags

			case "get-detection":
				epf = configGetDetectionFlags

			case "update-detection":
				epf = configUpdateDetectionFlags

			case "get-pipeline":
				epf = configGetPipelineFlags

			case "update-pipeline":
				epf = configUpdatePipelineFlags

			case "get-recognition":
				epf = configGetRecognitionFlags

			case "update-recognition":
				epf = configUpdateRecognitionFlags

			case "test-recognition":
				epf = configTestRecognitionFlags

			}

		case "system":
			switch epn {
			case "status":
				epf = systemStatusFlags

			case "start-detection":
				epf = systemStartDetectionFlags

			case "stop-detection":
				epf = systemStopDetectionFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "health":
			c := healthc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "healthz":
				endpoint = c.Healthz()
			case "readyz":
				endpoint = c.Readyz()
			}
		case "auth":
			c := authc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "login":
				endpoint = c.Login()
				data, err = authc.BuildLoginPayload(*authLoginBodyFlag)
			case "status":
				endpoint = c.Status()
			}
		case "camera":
			c := camerac.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
			case "get":
				endpoint = c.Get()
				data, err = camerac.BuildGetPayload(*cameraGetIDFlag)
			case "create":
				endpoint = c.Create()
				data, err = camerac.BuildCreatePayload(*cameraCreateBodyFlag)
			case "update":
				endpoint = c.Update()
				data, err = camerac.BuildUpdatePayload(*cameraUpdateBodyFlag, *cameraUpdateIDFlag)
			case "delete":
				endpoint = c.Delete()
				data, err = camerac.BuildDeletePayload(*cameraDeleteIDFlag)
			case "activate":
				endpoint = c.Activate()
				data, err = camerac.BuildActivatePayload(*cameraActivateIDFlag)
			case "deactivate":
				endpoint = c.Deactivate()
				data, err = camerac.BuildDeactivatePayload(*cameraDeactivateIDFlag)
			case "capture":
				endpoint = c.Capture()
				data, err = camerac.BuildCapturePayload(*cameraCaptureIDFlag)
			case "enable-alerts":
				endpoint = c.EnableAlerts()
				data, err = camerac.BuildEnableAlertsPayload(*cameraEnableAlertsIDFlag)
			case "disable-alerts":
				endpoint = c.DisableAlerts()
				data, err = camerac.BuildDisableAlertsPayload(*cameraDisableAlertsIDFlag)
			}
		case "motion":
			c := motionc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "events":
				endpoint = c.Events()
				data, err = motionc.BuildEventsPayload(*motionEventsCameraIDFlag, *motionEventsSinceFlag, *motionEventsLimitFlag)
			case "event":
				endpoint = c.Event()
				data, err = motionc.BuildEventPayload(*motionEventIDFlag)
			case "frame":
				endpoint = c.Frame()
				data, err = motionc.BuildFramePayload(*motionFrameIDFlag)
			case "forensic-thumbnail":
				endpoint = c.ForensicThumbnail()
				data, err = motionc.BuildForensicThumbnailPayload(*motionForensicThumbnailIDFlag, *motionForensicThumbnailIndexFlag)
			}
		case "config":
			c := configc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "get":
				endpoint = c.Get()
			case "update":
				endpoint = c.Update()
				data, err = configc.BuildUpdatePayload(*configUpdateBodyFlag)
			case "test-notification":
				endpoint = c.TestNotification()
			case "get-dinov3":
				endpoint = c.GetDinov3()
			case "update-dinov3":
				endpoint = c.UpdateDinov3()
				data, err = configc.BuildUpdateDinov3Payload(*configUpdateDinov3BodyFlag)
			case "test-dinov3":
				endpoint = c.TestDinov3()
			case "get-yolo":
				endpoint = c.GetYolo()
			case "update-yolo":
				endpoint = c.UpdateYolo()
				data, err = configc.BuildUpdateYoloPayload(*configUpdateYoloBodyFlag)
			case "test-yolo":
				endpoint = c.TestYolo()
			case "get-detection":
				endpoint = c.GetDetection()
			case "update-detection":
				endpoint = c.UpdateDetection()
				data, err = configc.BuildUpdateDetectionPayload(*configUpdateDetectionBodyFlag)
			case "get-pipeline":
				endpoint = c.GetPipeline()
			case "update-pipeline":
				endpoint = c.UpdatePipeline()
				data, err = configc.BuildUpdatePipelinePayload(*configUpdatePipelineBodyFlag)
			case "get-recognition":
				endpoint = c.GetRecognition()
			case "update-recognition":
				endpoint = c.UpdateRecognition()
				data, err = configc.BuildUpdateRecognitionPayload(*configUpdateRecognitionBodyFlag)
			case "test-recognition":
				endpoint = c.TestRecognition()
			}
		case "system":
			c := systemc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "status":
				endpoint = c.Status()
			case "start-detection":
				endpoint = c.StartDetection()
			case "stop-detection":
				endpoint = c.StopDetection()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// healthUsage displays the usage of the health command and its subcommands.
func healthUsage() {
	fmt.Fprintln(os.Stderr, `Health check endpoints for Kubernetes probes`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] health COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    healthz: Liveness probe endpoint - indicates if the service is alive`)
	fmt.Fprintln(os.Stderr, `    readyz: Readiness probe endpoint - indicates if the service is ready to serve traffic`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s health COMMAND --help\n", os.Args[0])
}
func healthHealthzUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] health healthz", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Liveness probe endpoint - indicates if the service is alive`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "health healthz")
}

func healthReadyzUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] health readyz", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Readiness probe endpoint - indicates if the service is ready to serve traffic`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "health readyz")
}

// authUsage displays the usage of the auth command and its subcommands.
func authUsage() {
	fmt.Fprintln(os.Stderr, `Authentication service for JWT token management`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] auth COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    login: Authenticate with username and password to receive a JWT token`)
	fmt.Fprintln(os.Stderr, `    status: Check authentication status and get current user info`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s auth COMMAND --help\n", os.Args[0])
}
func authLoginUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] auth login", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Authenticate with username and password to receive a JWT token`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "auth login --body '{\n      \"password\": \"Quia repudiandae neque magni.\",\n      \"username\": \"Eum cum et cumque.\"\n   }'")
}

func authStatusUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] auth status", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Check authentication status and get current user info`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "auth status")
}

// cameraUsage displays the usage of the camera command and its subcommands.
func cameraUsage() {
	fmt.Fprintln(os.Stderr, `Camera management service for video alarm system`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] camera COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: List all configured cameras`)
	fmt.Fprintln(os.Stderr, `    get: Get camera information by ID`)
	fmt.Fprintln(os.Stderr, `    create: Add a new camera`)
	fmt.Fprintln(os.Stderr, `    update: Update camera configuration. Device can only be changed when camera is inactive.`)
	fmt.Fprintln(os.Stderr, `    delete: Remove a camera`)
	fmt.Fprintln(os.Stderr, `    activate: Activate camera for motion detection`)
	fmt.Fprintln(os.Stderr, `    deactivate: Deactivate camera`)
	fmt.Fprintln(os.Stderr, `    capture: Capture a single frame from camera as base64`)
	fmt.Fprintln(os.Stderr, `    enable-alerts: Enable alerts for this camera. Detection pipeline will create events and send notifications.`)
	fmt.Fprintln(os.Stderr, `    disable-alerts: Disable alerts for this camera. Detection pipeline still runs for bounding boxes but no events are created or notifications sent.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s camera COMMAND --help\n", os.Args[0])
}
func cameraListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] camera list", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List all configured cameras`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "camera list")
}

func cameraGetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] camera get", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get camera information by ID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: Camera ID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "camera get --id \"823578ec-cd72-4281-9547-f4dc7fbb1da9\"")
}

func cameraCreateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] camera create", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Add a new camera`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "camera create --body '{\n      \"device\": \"Quia reprehenderit.\",\n      \"events_enabled\": true,\n      \"fps\": 3704841809906153186,\n      \"name\": \"Voluptas saepe nihil.\",\n      \"notifications_enabled\": true,\n      \"resolution\": \"Officia voluptatem mollitia sint id.\"\n   }'")
}

func cameraUpdateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] camera update", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Update camera configuration. Device can only be changed when camera is inactive.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -id STRING: Camera ID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "camera update --body '{\n      \"device\": \"Aut repudiandae.\",\n      \"events_enabled\": true,\n      \"fps\": 7013805482669149604,\n      \"name\": \"Aut ut nemo autem commodi sit et.\",\n      \"notifications_enabled\": true,\n      \"resolution\": \"Quos eveniet dolores aut tempora aliquam ducimus.\"\n   }' --id \"edd69d74-9ab2-47e2-aeaa-0de38da72a28\"")
}

func cameraDeleteUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] camera delete", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Remove a camera`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: Camera ID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "camera delete --id \"fca33111-2a6d-4f6c-9579-87dabbc9bb5a\"")
}

func cameraActivateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] camera activate", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Activate camera for motion detection`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: Camera ID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "camera activate --id \"50cd470f-e56f-40b7-a25e-8ce6e42a6814\"")
}

func cameraDeactivateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] camera deactivate", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Deactivate camera`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: Camera ID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "camera deactivate --id \"972c083e-6072-4da3-ad78-6b094c9f32f1\"")
}

func cameraCaptureUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] camera capture", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Capture a single frame from camera as base64`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: Camera ID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "camera capture --id \"4c062439-2c5b-4a40-b924-efe962e79f41\"")
}

func cameraEnableAlertsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] camera enable-alerts", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Enable alerts for this camera. Detection pipeline will create events and send notifications.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: Camera ID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "camera enable-alerts --id \"35a4fa3f-a043-44eb-8b5a-ce85e1163bf9\"")
}

func cameraDisableAlertsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] camera disable-alerts", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Disable alerts for this camera. Detection pipeline still runs for bounding boxes but no events are created or notifications sent.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: Camera ID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "camera disable-alerts --id \"30aeb0d5-46cf-4c84-b033-aca96173a6b7\"")
}

// motionUsage displays the usage of the motion command and its subcommands.
func motionUsage() {
	fmt.Fprintln(os.Stderr, `Motion detection and event management service`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] motion COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    events: List motion detection events`)
	fmt.Fprintln(os.Stderr, `    event: Get motion event by ID`)
	fmt.Fprintln(os.Stderr, `    frame: Get captured frame for motion event as base64`)
	fmt.Fprintln(os.Stderr, `    forensic-thumbnail: Get forensic face analysis thumbnail (NSA-style with landmarks) for a motion event`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s motion COMMAND --help\n", os.Args[0])
}
func motionEventsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] motion events", os.Args[0])
	fmt.Fprint(os.Stderr, " -camera-id STRING")
	fmt.Fprint(os.Stderr, " -since STRING")
	fmt.Fprint(os.Stderr, " -limit INT")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List motion detection events`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -camera-id STRING: `)
	fmt.Fprintln(os.Stderr, `    -since STRING: `)
	fmt.Fprintln(os.Stderr, `    -limit INT: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "motion events --camera-id \"66f585c5-f085-411f-9cc1-ae134b05e642\" --since \"1976-02-05T16:52:43Z\" --limit -3417244801850337904")
}

func motionEventUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] motion event", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get motion event by ID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: Event ID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "motion event --id \"b01cef36-c2c1-4549-a253-bf84a8756ae1\"")
}

func motionFrameUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] motion frame", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get captured frame for motion event as base64`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: Event ID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "motion frame --id \"ba48af73-a29d-45aa-9788-2d00ca07e876\"")
}

func motionForensicThumbnailUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] motion forensic-thumbnail", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprint(os.Stderr, " -index INT")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get forensic face analysis thumbnail (NSA-style with landmarks) for a motion event`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: Event ID`)
	fmt.Fprintln(os.Stderr, `    -index INT: Face thumbnail index (0-based)`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "motion forensic-thumbnail --id \"c7b96055-b8c6-4f8b-89f1-5c782912c3a0\" --index 5260664539340629944")
}

// configUsage displays the usage of the config command and its subcommands.
func configUsage() {
	fmt.Fprintln(os.Stderr, `System configuration management`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] config COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    get: Get current notification configuration`)
	fmt.Fprintln(os.Stderr, `    update: Update notification configuration`)
	fmt.Fprintln(os.Stderr, `    test-notification: Send test notification`)
	fmt.Fprintln(os.Stderr, `    get-dinov3: Get current DINOv3 AI configuration`)
	fmt.Fprintln(os.Stderr, `    update-dinov3: Update DINOv3 AI configuration`)
	fmt.Fprintln(os.Stderr, `    test-dinov3: Test DINOv3 service connectivity`)
	fmt.Fprintln(os.Stderr, `    get-yolo: Get current YOLO detection configuration`)
	fmt.Fprintln(os.Stderr, `    update-yolo: Update YOLO detection configuration`)
	fmt.Fprintln(os.Stderr, `    test-yolo: Test YOLO service connectivity`)
	fmt.Fprintln(os.Stderr, `    get-detection: Get combined detection configuration`)
	fmt.Fprintln(os.Stderr, `    update-detection: Update combined detection configuration`)
	fmt.Fprintln(os.Stderr, `    get-pipeline: Get detection pipeline configuration`)
	fmt.Fprintln(os.Stderr, `    update-pipeline: Update detection pipeline configuration`)
	fmt.Fprintln(os.Stderr, `    get-recognition: Get face recognition configuration`)
	fmt.Fprintln(os.Stderr, `    update-recognition: Update face recognition configuration`)
	fmt.Fprintln(os.Stderr, `    test-recognition: Test face recognition service connectivity`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s config COMMAND --help\n", os.Args[0])
}
func configGetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] config get", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get current notification configuration`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "config get")
}

func configUpdateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] config update", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Update notification configuration`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "config update --body '{\n      \"cooldown_seconds\": 5667207024610732359,\n      \"min_confidence\": 0.13495211,\n      \"telegram_bot_token\": \"Distinctio architecto odio.\",\n      \"telegram_chat_id\": \"Dolore itaque est.\",\n      \"telegram_enabled\": true\n   }'")
}

func configTestNotificationUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] config test-notification", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Send test notification`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "config test-notification")
}

func configGetDinov3Usage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] config get-dinov3", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get current DINOv3 AI configuration`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "config get-dinov3")
}

func configUpdateDinov3Usage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] config update-dinov3", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Update DINOv3 AI configuration`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "config update-dinov3 --body '{\n      \"confidence_threshold\": 0.016302783,\n      \"enable_scene_analysis\": true,\n      \"enabled\": false,\n      \"fallback_to_basic\": true,\n      \"motion_threshold\": 0.69391114,\n      \"service_endpoint\": \"Debitis animi ipsam non.\"\n   }'")
}

func configTestDinov3Usage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] config test-dinov3", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Test DINOv3 service connectivity`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "config test-dinov3")
}

func configGetYoloUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] config get-yolo", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get current YOLO detection configuration`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "config get-yolo")
}

func configUpdateYoloUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] config update-yolo", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Update YOLO detection configuration`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "config update-yolo --body '{\n      \"box_color\": \"Ut omnis error rerum est et magni.\",\n      \"box_thickness\": 3083532263034087331,\n      \"classes_filter\": \"Autem eaque dignissimos rerum.\",\n      \"confidence_threshold\": 0.45829856,\n      \"draw_boxes\": false,\n      \"enabled\": false,\n      \"security_mode\": true,\n      \"service_endpoint\": \"Dolorum deserunt ipsum corporis dolores ullam.\",\n      \"tasks\": [\n         \"detect\",\n         \"pose\"\n      ]\n   }'")
}

func configTestYoloUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] config test-yolo", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Test YOLO service connectivity`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "config test-yolo")
}

func configGetDetectionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] config get-detection", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get combined detection configuration`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "config get-detection")
}

func configUpdateDetectionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] config update-detection", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Update combined detection configuration`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "config update-detection --body '{\n      \"dinov3\": {\n         \"confidence_threshold\": 0.8039321,\n         \"enable_scene_analysis\": true,\n         \"enabled\": true,\n         \"fallback_to_basic\": true,\n         \"motion_threshold\": 0.017921131,\n         \"service_endpoint\": \"Impedit dolore.\"\n      },\n      \"fallback_enabled\": true,\n      \"primary_detector\": \"dinov3\",\n      \"yolo\": {\n         \"box_color\": \"Fugit saepe voluptatem.\",\n         \"box_thickness\": 297499421035054028,\n         \"classes_filter\": \"Nesciunt ab corrupti nulla minus.\",\n         \"confidence_threshold\": 0.63516176,\n         \"draw_boxes\": true,\n         \"enabled\": true,\n         \"security_mode\": true,\n         \"service_endpoint\": \"Autem accusantium vel.\",\n         \"tasks\": [\n            \"detect\",\n            \"pose\"\n         ]\n      }\n   }'")
}

func configGetPipelineUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] config get-pipeline", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get detection pipeline configuration`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "config get-pipeline")
}

func configUpdatePipelineUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] config update-pipeline", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Update detection pipeline configuration`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "config update-pipeline --body '{\n      \"detectors\": [\n         \"yolo\",\n         \"face\"\n      ],\n      \"execution_mode\": \"sequential\",\n      \"mode\": \"visual_only\",\n      \"motion_cooldown_seconds\": 8450747842556392749,\n      \"motion_sensitivity\": 0.29052767,\n      \"schedule_interval\": \"Quae nostrum velit.\"\n   }'")
}

func configGetRecognitionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] config get-recognition", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get face recognition configuration`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "config get-recognition")
}

func configUpdateRecognitionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] config update-recognition", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Update face recognition configuration`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "config update-recognition --body '{\n      \"box_thickness\": 4809409892930057921,\n      \"enabled\": false,\n      \"known_face_color\": \"Reprehenderit facilis atque consectetur quibusdam iste est.\",\n      \"service_endpoint\": \"Quia quas.\",\n      \"similarity_threshold\": 0.20452018,\n      \"unknown_face_color\": \"Aut voluptatum error doloribus et eos.\"\n   }'")
}

func configTestRecognitionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] config test-recognition", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Test face recognition service connectivity`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "config test-recognition")
}

// systemUsage displays the usage of the system command and its subcommands.
func systemUsage() {
	fmt.Fprintln(os.Stderr, `System status and monitoring`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] system COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    status: Get overall system status`)
	fmt.Fprintln(os.Stderr, `    start-detection: Start motion detection on all active cameras`)
	fmt.Fprintln(os.Stderr, `    stop-detection: Stop motion detection`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s system COMMAND --help\n", os.Args[0])
}
func systemStatusUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] system status", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get overall system status`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "system status")
}

func systemStartDetectionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] system start-detection", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Start motion detection on all active cameras`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "system start-detection")
}

func systemStopDetectionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] system stop-detection", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Stop motion detection`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "system stop-detection")
}
